extends layout

block content
    include includes/_header.pug
    .container
        .row
            form(action="/admin", method="post")
                h3 Delete Post by ID
                <input type="text" class="form-control" id="exampleInputPassword1" placeholder="ID", name="value">
                button(type="submit") Submit
            if smg
                h1 #{smg}

